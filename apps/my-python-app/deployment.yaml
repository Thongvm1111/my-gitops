apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-python-app
  namespace: production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-python-app
  template:
    metadata:
      labels:
        app: my-python-app
    spec:
      containers:
        - name: my-python-app
          image: nginx:latest
          ports:
            - containerPort: 80
